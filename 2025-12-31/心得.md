# 二分查找原理(704)
1.对于左闭右闭：\
  `while(left<=right)`因为[1,1]是合法的，只不过是只含有一个元素\
  `left=middle+1`左边界更新\
  `right=middle-1`右边界更新\
2.对于左闭右开：\
  `while(left<right)`因为[1,1)不合法\
  `left=middle+1`左边界更新\
  `right=middle`右边界更新\
3.属于logn的算法
